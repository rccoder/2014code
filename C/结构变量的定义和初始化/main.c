#include <stdio.h>
#include <stdlib.h>

int main()
{
    struct friends_list{
        char name[10];
        int age;   /*先定义结构类型，在定义具有这种结构类型的变量*/
        char tel[13];
    };
    struct friends_list friend1, friend2
    return 0;
}
